# =============================================================================
# SOAR 平台 — 生产 Nginx 配置（入库版本化）
#
# 架构：
#   浏览器 → Nginx(:8080, HTTPS) ── 托管 /opt/soar-src/frontend/dist
#                                └─ /api/ 与 /ws/ ──> uvicorn(127.0.0.1:8001, 纯 HTTP)
# =============================================================================

# HTTP → HTTPS 强制跳转（HSTS 生效前提）
server {
    listen 80;
    server_name _;
    return 301 https://$host:8080$request_uri;
}

# HTTPS 生产入口：TLS 终止在此完成
server {
    listen 8080 ssl;
    server_name _;

    ssl_certificate     /etc/nginx/ssl/server.crt;
    ssl_certificate_key /etc/nginx/ssl/server.key;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305;
    ssl_prefer_server_ciphers on;
    ssl_session_timeout 1d;
    ssl_session_cache shared:SSL:10m;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Content-Security-Policy "default-src 'self'; script-src 'nonce-$request_id' 'strict-dynamic'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self' data:; connect-src 'self' wss:; object-src 'none'; base-uri 'self'; frame-ancestors 'none';" always;
    sub_filter_once off;
    sub_filter '<script' '<script nonce="$request_id"';

    client_max_body_size 50M;

    root /opt/soar-src/frontend/dist;
    index index.html;

    location ~* /\.(git|env|svn|hg|htaccess) { deny all; }
    location ~* /(adpassword|password|secret|credential|\.env).*\.(txt|json|log|bak|env)$ { deny all; }
    location ~* \.(bak|old|orig)$ { deny all; }
    location ~* ^/(src|node_modules|public|tests?)/ { deny all; }

    # ^~ 优先于静态后缀正则，避免 /api/*.png 附件被当成 dist 静态文件
    location ^~ /api/ {
        proxy_pass http://127.0.0.1:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_read_timeout 600s;
        proxy_send_timeout 600s;
        proxy_request_buffering off;
        proxy_buffering off;
        proxy_cache off;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        chunked_transfer_encoding on;
    }

    location ^~ /ws/ {
        proxy_pass http://127.0.0.1:8001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    location = /assets {
        try_files /index.html =404;
    }
    location = /assets/ {
        try_files /index.html =404;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(js|css|png|jpg|svg|woff2?)$ {
        expires 1d;
        add_header Cache-Control "public, no-transform";
    }
}
